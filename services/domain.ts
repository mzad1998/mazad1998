import {AppData,ServiceRequest,WorkshopOffer} from '@/types'; import {workshops} from '@/mocks/workshops';
export const id=(p:string)=>`${p}-${Date.now()}-${Math.random().toString(36).slice(2,8)}`;
export function offersFor(request:ServiceRequest):WorkshopOffer[]{const base={maintenance:85000,parts:65000,mobile:70000,crane:55000,wash:25000,accessories:45000}[request.category];return workshops.slice(0,Math.min(7,5+(request.id.charCodeAt(0)%3))).map((w,i)=>({id:id('offer'),requestId:request.id,workshopId:w.id,priceMin:base+i*12500,priceMax:i%3===0?base+i*12500+30000:undefined,availability:i%2?'غداً الساعة ١٠ صباحاً':'متاح اليوم',duration:i%3?'٢–٣ ساعات':'يوم عمل',partsIncluded:i%2===0,warranty:w.warranty,message:['نفحص السيارة أولاً ونوضح لك كل خطوة قبل البدء.','متوفر لدينا فريق متخصص ويمكن إنجاز الطلب بالموعد المحدد.','العرض يشمل فحص الجودة بعد إكمال العمل.'][i%3]!,includedWork:'الفحص، التنفيذ، واختبار الجودة',images:[],createdAt:new Date(Date.now()+i).toISOString()}));}
export function acceptOffer(data:AppData,offerId:string):AppData{const offer=data.offers.find(o=>o.id===offerId);if(!offer)throw new Error('العرض غير موجود');if(data.acceptedOffers.some(a=>a.requestId===offer.requestId))throw new Error('تم قبول عرض لهذا الطلب مسبقاً');const now=new Date().toISOString();return {...data,acceptedOffers:[...data.acceptedOffers,{id:id('accepted'),requestId:offer.requestId,offerId,acceptedAt:now}],requests:data.requests.map(r=>r.id===offer.requestId?{...r,status:'accepted',updatedAt:now,timeline:[...r.timeline,{id:id('event'),status:'accepted',label:'تم قبول عرض',createdAt:now}]}:r),notifications:[{id:id('note'),type:'accepted',title:'تم قبول العرض',body:'تم تثبيت اختيارك ويمكنك الآن التواصل مع الورشة.',requestId:offer.requestId,read:false,createdAt:now},...data.notifications]};}
