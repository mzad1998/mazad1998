import {Text} from 'react-native';import {router,useLocalSearchParams} from 'expo-router';import {Button,Card,Empty,Screen,ui} from '@/components/ui';import {useApp} from '@/services/AppProvider';
export default function AdvertisementDetails(){const{id}=useLocalSearchParams<{id:string}>();const ad=useApp().data.advertisements.find(x=>x.id===id&&x.active);return <Screen title="تفاصيل الإعلان">{ad?<Card><Text style={ui.title}>{ad.title}</Text><Text style={ui.h2}>{ad.subtitle}</Text><Text style={ui.muted}>{ad.details}</Text></Card>:<Empty title="لا يوجد إعلان نشط" body="انتهت مدة هذا الإعلان أو لم يعد متاحاً."/>}<Button title="العودة للرئيسية" onPress={()=>router.replace('/home')}/></Screen>}
